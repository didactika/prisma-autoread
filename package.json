{
  "name": "@didactika/prisma-autoread",
  "version": "0.1.0",
  "description": "Auto-generate Express GET list endpoints from your Prisma schema — type-aware filtering, pagination, search, and relation includes from the query string, zero boilerplate.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.js",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "jest",
    "test:unit": "jest --testPathPattern=tests/unit",
    "test:integration": "jest --testPathPattern=tests/integration",
    "test:e2e": "prisma generate && prisma db push --force-reset && jest --testPathPattern=tests/e2e",
    "test:coverage": "jest --coverage",
    "prepublishOnly": "npm run test && npm run build"
  },
  "keywords": [
    "prisma",
    "express",
    "rest",
    "filter",
    "pagination",
    "middleware",
    "autoread"
  ],
  "author": "Didactika <info@didactika.org>",
  "contributors": [
    "Eduardo Estrada <me@e2rd0.com>"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/didactika/prisma-autoread.git"
  },
  "homepage": "https://github.com/didactika/prisma-autoread#readme",
  "bugs": {
    "url": "https://github.com/didactika/prisma-autoread/issues"
  },
  "peerDependencies": {
    "@prisma/client": ">=6.0.0",
    "express": ">=4.0.0"
  },
  "dependencies": {
    "halson": "^3.1.0",
    "http-response-client": "^1.1.0"
  },
  "devDependencies": {
    "@prisma/client": "^6.0.0",
    "@types/express": "^5.0.0",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.0",
    "express": "^4.21.2",
    "jest": "^29.0.0",
    "prisma": "^6.0.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.0.0"
  }
}